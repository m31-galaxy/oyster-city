# TfL Unified API key — register free at https://api-portal.tfl.gov.uk
# Auth is the app_key query param ONLY. The old app_id is deprecated and is
# NOT required. The key is read server-side only and never reaches the browser.
# Copy this file to .env.local and fill in your key.
TFL_APP_KEY=
